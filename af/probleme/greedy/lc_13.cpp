// greedy - Minimum Subsequence in Non-Increasing Order
#include <algorithm>
#include <iostream>
#include <vector>

std::vector<int> minSubsequence(std::vector<int> &nums) {
    std::vector<int> res;

    return res; 
}

int main() {
    std::vector<int> v;

    std::vector<int> res = minSubsequence(v);
    for (auto e : res) {
        std::cout << e << ' ';
    }
    std::cout << '\n';
    return 0;
}
